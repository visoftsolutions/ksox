const config = {
  trailingComma: "all",
  tabWidth: 2,
  useTabs: false,
  semi: true,
  singleQuote: false,
  printWidth: 160,
  plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
