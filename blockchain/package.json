{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "type": "module",
  "scripts": {
    "prettier": "prettier -w .",
    "lint": "eslint . --ext .tsx --config packages/eslint/.eslintrc.cjs",
    "update": "npx npm-check-updates -u -ws --root"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "@app/*",
    "packages/*"
  ]
}
