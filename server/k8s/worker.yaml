apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-worker-deployment
  labels:
    app: server-worker
spec:
  selector:
    matchLabels:
      app: server-worker
  template:
    metadata:
      labels:
        app: server-worker
    spec:
      containers:
      - name: server-worker
        image: server-worker
        ports:
        - containerPort: 80
        env:
        - name: DATABASE_URL
          value: postgresql://postgres:postgres@postgres-postgresql/postgres
        - name: ENGINE_URL
          value: http://server-engine-service/
        - name: REDIS_URL
          value: redis://redis-replicas/
---
apiVersion: v1
kind: Service
metadata:
  name: server-worker-service
spec:
  selector:
    app: server-worker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: server-worker-ingress
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
  - http:
      paths:
      - path: /api(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: server-worker-service
            port:
              number: 80
