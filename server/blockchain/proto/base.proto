syntax = "proto3";
package server.blockchain.base;

message WithdrawRequest {
    string maker_address = 1;
    string taker_address = 2;
    string asset_address = 3;
    string amount = 4;
}

message WithdrawResponse {}

service Blockchain {
    rpc Withdraw(WithdrawRequest) returns (WithdrawResponse) {}
}