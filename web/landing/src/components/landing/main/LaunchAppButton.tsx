import { A } from "solid-start";

export default function LaunchAppButton() {
  return (
    <a href="/app/">
      <div class="rounded-full bg-primary p-[10px_32px] text-center font-lexend text-hero-button font-medium text-text-1">
        Launch App
      </div>
    </a>
  );
}
