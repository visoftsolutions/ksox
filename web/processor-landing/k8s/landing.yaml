apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-processor-landing-deployment
  labels:
    app: web-processor-landing
spec:
  selector:
    matchLabels:
      app: web-processor-landing
  template:
    metadata:
      labels:
        app: web-processor-landing
    spec:
      containers:
        - name: web-processor-landing
          image: web-processor-landing
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "128Mi"
              cpu: "500m"
            limits:
              memory: "256Mi"
              cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: web-processor-landing-service
spec:
  selector:
    app: web-processor-landing
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-processor-landing-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
spec:
  tls:
    - hosts:
        - ksox.finance
        - pay.ksox.finance
      secretName: ksox-finance-tls
  rules:
    - host: pay.ksox.finance
      http:
        paths:
          - path: /(.*)
            pathType: Prefix
            backend:
              service:
                name: web-processor-landing-service
                port:
                  number: 80
