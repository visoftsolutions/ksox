{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "type": "module",
  "scripts": {
    "lint": "eslint --config packages/eslint/.eslintrc.cjs '**/*.{js,jsx,ts,tsx}'",
    "lint:fix": "eslint --config packages/eslint/.eslintrc.cjs --fix '**/*.{js,jsx,ts,tsx}'",
    "format": "prettier --check '**/*.{js,jsx,ts,tsx,json,css,md}'",
    "format:fix": "prettier --check --write '**/*.{js,jsx,ts,tsx,json,css,md}'",
    "update": "npx npm-check-updates -ws -u --root"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
