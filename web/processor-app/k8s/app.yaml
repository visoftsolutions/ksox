apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-processor-app-deployment
  labels:
    app: web-processor-app
spec:
  selector:
    matchLabels:
      app: web-processor-app
  template:
    metadata:
      labels:
        app: web-processor-app
    spec:
      containers:
        - name: web-processor-app
          image: web-processor-app
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "128Mi"
              cpu: "500m"
            limits:
              memory: "256Mi"
              cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: web-processor-app-service
spec:
  selector:
    app: web-processor-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-processor-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
spec:
  tls:
    - hosts:
        - ksox.finance
        - pay.ksox.finance
      secretName: ksox-finance-tls
  rules:
    - host: pay.ksox.finance
      http:
        paths:
          - path: /app(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: web-processor-app-service
                port:
                  number: 80
