apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-ksox-app-deployment
  labels:
    app: web-ksox-app
spec:
  selector:
    matchLabels:
      app: web-ksox-app
  template:
    metadata:
      labels:
        app: web-ksox-app
    spec:
      containers:
        - name: web-ksox-app
          image: web-ksox-app
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "128Mi"
              cpu: "500m"
            limits:
              memory: "256Mi"
              cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: web-ksox-app-service
spec:
  selector:
    app: web-ksox-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-ksox-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
spec:
  tls:
    - hosts:
        - ksox.finance
        - www.ksox.finance
      secretName: ksox-finance-tls
  rules:
    - host: www.ksox.finance
      http:
        paths:
          - path: /app(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: web-ksox-app-service
                port:
                  number: 80
